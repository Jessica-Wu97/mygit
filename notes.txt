mkdir
cd
git init
git add<file>
git commt -m"log"
git log
git reset --hard HEAD^
git reset --hard HEAD^^
git reset --hard HEAD~n
git reset --hard"versionID"
git status
cat <file>
this line can't be committed.
-branch
--command
git checkout -- <file> "Attention! There is a space between -- and <file>"